<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank You for shopping at Jungle </h1>
    <h3>Your order number are <%= @order.id %> </h3>
    <p>
      Hello <%= @order.email %>
      you have bought 
      <% @order.line_items.all.each do |item| %>
        <%= render 'item', item:item%>
        
      <% end %>


      the total is <%= @order.total_cents/100.0 %>
    </p>
    <p>
      To login to the site, just follow this .
    </p>
    <p>Thanks for joining and have a great day!</p>
  </body>
</html>